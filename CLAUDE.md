# CLAUDE.md — Kristy Grocery-Coach Overhaul

Working branch: `overhaul/grocery-coach`. This file holds the non-negotiables for the overhaul. They apply to **every** step and must survive a `/clear`. The step-by-step plan lives in `BUILD_QUEUE.md`; the full brief in `OVERHAUL.md`; the condition-focus + education extension in `STEP1b_focuses_and_education_prompt.md`.

## What we're building

Repositioning Kristy from a chat-first nutrition logger into a **grocery coach**. Scan is the front door, chat is the moat, and the primary nav is three moments: **List (before) · Scan (in aisle) · Haul (after)**. The coaching relationship stays — it just anchors to concrete artifacts (a scan, a haul, a list) instead of a blank chat box.

## Non-negotiables (inherit these on every step)

1. **Reuse the locked brand. Never invent.** Near-black forest green, gold accents, Playfair Display + Inter, thin gold thread/dot motif. Import from the centralized tokens module (Step 0). Kristy's coaching/spoken text = `kristyVoice` (Playfair italic); all factual/UI/ingredient text = Inter. Never substitute a color or face.

2. **The claim lock is law.** Every health/ingredient claim comes from a matched entry in `kristy_ingredient_knowledge_base.json`. The model may rephrase tone but may NEVER introduce a concern, cancer link, or claim not in the data it was given. Enforce structurally (strip entries to allowed fields before the model call), not just in the prompt. This is the liability shield — it holds on every surface that speaks in Kristy's voice: verdict note, haul read, list reasoning, chat.

3. **No-treatment rule.** Kristy is a coach, not a doctor. Dietary focuses are preferences the user turns on about themselves, never inferences. Never claim to treat/manage/lower/reverse/cure a condition, never state or imply the user has a diagnosis, never give a medical directive. Preference framing only ("you're watching sodium…").

4. **The stamp is earned.** The gold "Kristy Approved" seal renders only when `tier === 'approved'`. Every tier below gets a plain verdict bar. Never show the seal for a swap or skip.

5. **Don't reshape the Step 1 engine output.** The matched-entry object shape from `server/lib/verdictEngine.js` is consumed directly by the note composer and the card. Extend it additively if needed; never restructure it — the tests and the claim lock depend on it.

6. **Preserve what works.** Guest mode (IP rate-limited, soft-gated at the 4-exchange threshold), phone OTP auth (Supabase + Twilio), the `isPremium()` gate (provider-agnostic: RevenueCat for Apple IAP, Stripe on web), USDA macro logging, weight logging + adaptive TDEE, Vercel Web Analytics, privacy policy / ToS, 7-day memory context, the weekly Sunday-summary and proactive-insight triggers. This is a repositioning of the surface and value prop, not a rewrite of the plumbing.

## Architecture

The verdict engine is authoritative and lives **server-side** on Railway (`kristy-server`): KB + matching + tier scoring + the one claim-locked Haiku note call, behind `POST /verdict`. The web SPA and the Expo/RN app are thin clients that render the returned verdict object. Build the web SPA first as the reference; the RN port for App Store submission comes after the queue.

## Workflow

- One step at a time, in `BUILD_QUEUE.md` order. Build → run the step's acceptance check → commit → continue.
- One commit per step, message prefixed `feat(<area>): … (Step N)`.
- Pause only if an acceptance check fails or you hit a real decision. Don't ask permission between passing steps.
- If a git object write fails with "permission denied," it's the OneDrive sync locking `.git` — retry the commit; if it persists, stop and tell me. Never hand-edit the KB or committed files to recover.
- Verify against the engine + live scripts, not the old client (the old scan path 400s against the new `/verdict` contract until Step 4 repoints it — that's expected mid-overhaul).

## Status

Keep this current as steps land.
- ✅ Step 0 — design tokens centralized
- ✅ Step 1 — KB-driven verdict engine (`caedf1f`)
- ✅ Step 2 — `/verdict` + claim-locked Haiku note (`97c8ea0`)
- ✅ Step 3 — scan verdict card (`client/src/components/ScanVerdictCard.jsx`)
- ✅ Step 4 — scan paths repointed to `/verdict` (barcode + label-vision extraction → card; OFF→vision fallback; `server/routes/scan.js`, `server/lib/{scanExtract,labelVision}.js`, client `runProductScan` + `ScanSheet`)
- ✅ Step 5 — three-moment nav List·Scan·Haul (`BottomNav`, Scan centered/primary opens camera; chat demoted; guest scanning live w/ universal layer; `ScanHome`, `MomentStub`)
- ✅ Step 6 — 60-second onboarding: goal + non-negotiables → `user_goals.{coach_goal,non_negotiables}` (new `/api/onboarding/coach` + trial), header `GoalChip`, config-driven `coachGoals.js` (focuses append-ready), first-scan payoff. Feeds goal+nonNegotiables into every /verdict. ~~Grocery onboarding is now the gate~~ → **CORRECTION (Block F):** this shipped **orphaned** — `saveCoachProfile` / `/api/onboarding/coach` had no client caller and goals were set via `saveProfileFields`, which never starts the trial, so grocery users never got the trial. Block F wired it as a **skippable first-run** (not a hard gate). TDEE `Onboarding.jsx` preserved. ⚠️ DB persist path not live-exercised (no Supabase in env); goal→note change verified live.
- ✅ Hardening — scan input pipeline: (1) barcode miss / unreadable auto-pivots to the label-photo path (`ScanSheet.onLabelFile`); (2) language guard (`pickEnglishText` + `looksNonEnglish`) rejects non-English OFF text AND foreign vision transcription so an unreadable product never gets a silent stamp. Verified: guard unit + live invariant (extraction never returns a foreign string); English products still resolve.
- ✅ Focuses + Education extension (A–D): KB condition tags (`glycemic_impact`, `cardiovascular_relevance`); engine thresholds (`SODIUM_HIGH`/`ADDED_SUGAR_HIGH`; sat-fat read-not-penalized) + bounded focus escalation (never skip via focus, no fabrication, stamp preserved, focus-relevant surfaces first); onboarding focus multi-select + one-time coach-not-doctor disclaimer + `focuses` persistence; note guardrail (preference framing, no treat/manage/diagnose, traces to KB/nutrition); `kristy_education.json` + one-ism-per-card selection + ambient rotation. Verified: 24 unit + `extension.livetest.js` (engine/education/live note) + client render checks.
- ✅ Step 7 — Haul surface: scans recorded to `haul_scans`; `HaulMoment` shows trip/week distribution bar (approved/note/swap), scrollable item list w/ tiers, and Kristy's weekly read (`generateHaulRead` — claim-locked, kristyVoice, next-week nudge). Actions: Add to next list (→ `kristy:nextList`, feeds Step 8), Share haul (web share; Step 10 adds the branded card). Verified: `haul.livetest.js` (distribution + live read) + render checks.
- ✅ Step 8 — List builder (HYBRID line, user-chosen): `ListMoment` — goal-templated checklist (`lib/list.js`) minus non-negotiables, with Haul swaps prepended; add/remove/check, persisted. Learning signals (keep/remove/accepted-swap) logged from day one → removed items never re-suggested; sets up the later scoring upgrade over hauls/memory/training with no rebuild. ~~Client-only (localStorage) for now.~~ → **Server-persisted as of Block F** (`shopping_lists` + `/api/list`, generation + gating moved server-side); localStorage is now a cache. Verified: generation/filter/learning + render checks.
- ✅ Step 9 — chat as connective tissue: no blank box. `ChatLauncher` offers concrete artifact entry points (scan/haul/list) when the thread is empty; each artifact (verdict card, Haul, List) has "Ask Kristy about this" → `openChat` seeds an AI opener grounded in that artifact (rides in conversationHistory → memory/rate-limit/errors unchanged via `/api/chat`). Meal logging + coaching preserved. Verified: launcher + affordance renders; build.
- ✅ Step 10 — shareable haul card: `haulCanvas.js` draws the scorecard to a branded 1080×1350 PNG (forest green + gold, thread/dot, distribution + legend + Kristy's read + wordmark + CTA); `HaulShareCard` overlay wires Share → web share sheet (PNG file). "Hide personal data" toggle strips the read + switches counts to %; branding stays. Verified: recording-ctx draw checks + build.
- ✅ Step 11 — repositioned free/paid gate on the provider-agnostic `isPremium` (RevenueCat + Stripe, trial preserved). Free = scan + universal layer, always; paid = personalized note + focus escalation, Haul weekly read, List builder. Twist: first 3 personalized notes free regardless of trial state (lifetime `free_notes_used` counter), then gated. Upsell + Upgrade copy name the specific value in Kristy's voice (not "go premium"). Verified: `premium.livetest.js` (3-tastes→gate, provider-agnostic) + client gating renders; build.
- ✅ Step 12 — final audit delivered (findings ranked; no fixes auto-committed). Fixes since, per user direction: H-1 (`791786c`) chat prompt no-treatment/no-diagnosis guardrail + "coach not doctor" reframe (live-verified redirect); M-1 + M-2 (this commit) analytics `track()` on scan/verdict/haul-share/list-build + guest scan card sign-in nudge. Deferred (user): L-1..L-4 (a11y/soft-gate polish), L-2 won't-fix (counter race favors user). Live-smoke TODO (needs migration applied): onboarding/haul_scans/free_notes_used persistence; ~~List is localStorage-only~~ → List **now server-persisted** (Block F, `shopping_lists`) but its migration is likewise not yet live-exercised.
- ℹ️ Scan stress-test (`scan.stresstest.js`): #1 failure = OFF-US barcode coverage gap (~40% hit); vision path robust. Both proposed fixes now built (above). Remaining ops idea: OFF backoff+cache so throttling ≠ false-missing.

### Post-queue blocks (A–F)
- ✅ Block A — favicon set generated from the hair silhouette (`client/tools/make_favicons.py`, reproducible): 16/32/48/180 + `.ico`, forest ground baked in. Small sizes simplify by *thickness* (open the interior dark regions so sub-pixel strand gaps fill; seed-and-regrow the negative-space face) rather than blurring, which blobs the contour. Wired into app/landing/privacy/terms + manifest. Leaf emoji gone repo-wide, incl. the RN entry loader (`984edac`).
- ✅ Block B — verdict card was already listing every matched flag; the KB one_liners and the 5 `history` entries were already why-first and verbatim. Real work was two defects found while verifying: `severityColor` mapped high+moderate to the same gold (4 severities, 3 dots) and guests got untappable flag rows despite the endpoint being public. `scripts/ingredient.livetest.js` (`60148ae`).
- ✅ Block C — **hard lines are now deterministic** (`server/lib/hardLines.js`): they were prompt-only, so "no seed oils" never actually blocked anything. A declared line resolves to KB ids, escalates on the bounded ladder, and withholds the seal; custom lines are `kb:<id>` from a name+alias search. `gluten-free`/`dairy-free` stay ADVISORY — the KB has no such data and claiming to check it would be fabrication. Hard lines apply on free + gated paths (KB read, no model call). Taxonomy +6 goals/+4 focuses/+8 lines, every new focus backed by a real KB category or nutrition field. Free-text intake maps onto the enum and is re-filtered against it server-side (`preferenceMap.js`). Premium reachable in one tap from 6 places; chat given its own persistent affordance above the moment row (not a 4th tab — the three moments are a sequence); List tags Kristy-added/haul items; first-ever `max-width` breakpoint at 400px. `scripts/preferences.livetest.js` (`6d68e42`, `3215ab7`).
- ✅ Block D — landing on the forest ground (flat, not a page gradient: body's background propagates to the canvas and sizes from the root box); all four beat visuals in a phone silhouette; Yuka named once in the hero (+ verbatim meta), comparison generic; tier cards carry the framing sentence + explicit verdicts; gym register retired; 430px breakpoint (`766db53`).
- ⚠️ **Verify mobile over CDP, not `--window-size`.** Chrome enforces a ~500px minimum window on Windows: `--window-size=390` renders at 504 and crops the screenshot, which looks exactly like horizontal overflow. Use `Emulation.setDeviceMetricsOverride` (see the scratchpad `shot.mjs` pattern).
- ✅ Block E — **the fat philosophy + the Time-tested tier** (`dc3e863`, this commit). Two halves of one idea: the real source beats the industrial imitation, and history is a legitimate — but honestly labeled — kind of evidence.
  - **Margarine resolved** (closes the old open decision): its own `seed_oil` entry, `high`/`kristys_standard`/`skip`, on its REAL modern composition (refined seed oils, colored and flavored to imitate butter). Still deliberately NOT aliased onto `partially_hydrogenated_oil` — US margarine was reformulated PHO-free, so a trans-fat claim would be false. `category: seed_oil` means the "no seed oils" hard line (now 10 ids) and both fat focuses catch it for free.
  - **The KB is no longer flag-only.** `polarity: "affirming"` (absent = concern, so all 67 prior entries are untouched) + the `time_tested` evidence tier. This was structurally necessary, not cosmetic: every `severity_level` is a CONCERN level, so a positive entry in `matched` scores as a concern — the mildest possible severity still yields `approved_with_note`, i.e. **it would strip the seal from any product containing garlic.** `matched` therefore stays concerns-only and affirmations ride in a new `affirmed` / `affirmationLayer`. Nothing consuming `matched` changed (non-negotiable #5 intact).
  - **Whole-food fats are clean because the KB holds no entry for them.** Locked with a regression test — the tripwire that stops any future entry (positive OR negative) from matching butter/ghee/tallow and costing a clean product its stamp. This is why EVOO is affirmed only in its explicit form.
  - **Affirmation scoping** — a badge only when the food IS the product: dominant (first) ingredient or a ≤3-token list. Honey buried in a granola bar gets nothing. Added-sugar escalation stays fully independent.
  - **Only unambiguous whole-food forms affirm.** Affirming entries are excluded from REVERSE matching, because reverse resolves a token UP to a longer alias — bare `"olive oil"` would have silently become `"extra virgin olive oil"` and earned a badge the label never gave. Bare olive/coconut/avocado oil is neither flagged nor affirmed. Don't affirm what you can't verify.
  - **Tradition may justify food-worth ONLY, never a health outcome.** Enforced in the note prompt and structurally: `sanitizeAffirmed` withholds `why`, `kristy_note`, AND `history` — history is the richest source of a tempting "used as a remedy for…" claim. Claim-lock test plants a cure string and proves it never reaches the model. No conspiracy/anti-science framing, ever.
  - **No health-outcome claim in EITHER direction** — the guardrail forbade *curing* but not *causing*, so "seed oils cause heart disease" passed. Now symmetric; the objection is rooted in processing, which is checkable. The saturated-fat line is allowed strictly as her read of a contested literature, never as a finding.
  - **The vision/photo prompt was a genuine split** — `lib/verdict.js` forbade the exact framing the KB path uses ("FORBIDDEN framing: 'seed oils — avoid' … any ingredient-toxicity claim not backed by mainstream dietetics consensus"). Kristy is now the same coach on both scan paths; moralizing words stay banned, `clean` un-banned (she demonstrably uses it).
- ℹ️ Two pre-existing alias collisions (harmless, exact/longest-first priority resolves them): `partially hydrogenated soybean oil` (soybean_oil vs PHO) and `bleached flour` (enriched_bleached_flour vs bleached_flour).
- ✅ Block F — **audit-driven front-door + durability fixes** (four commits, one each). A read-only as-built audit found the grocery reposition's front door was never actually wired — the coach onboarding + 7-day trial were **orphaned** — so grocery users never got a trial and Kristy never learned who she was coaching. Fixed in order:
  - **Coach onboarding + trial wired** (`3e921fb`): first run for a signed-in, goal-less user opens a Kristy-voiced `CoachOnboarding` (goal + optional focuses + hard lines) → `saveCoachProfile` → `/api/onboarding/coach` → `ensureTrial` grants the 7-day trial (previously unreachable). Skippable, remembered per-user (`kristy:coachOnbSkipped:<uid>`): skipping = goal-less, universal verdicts, **no trial until a goal is set**. ANY goal-set (onboarding, chip switcher, in-card ask) now routes through the trial-granting path (`persistGoal` → `saveCoachProfile`); `ensureTrial` is idempotent so switching goals never resets it. Header chip stays the anytime editor — no longer the only door in. `CoachOnboarding.jsx`, render branch in `App.jsx`.
  - **Guest work survives conversion** (`4e3b1e9`): guest scans were discarded when `GuestApp` unmounted on OTP verify — the user's most persuasive artifact, thrown away at the moment they convert. New `client/src/lib/guestState.js` keeps the last 10 guest scans (+ any goal) under `kristy:guest`; on the first authed session `App.replayGuestScans` posts them into the account's Haul (`saveHaulScan`) and clears the key, ref-guarded against repeat auth events. A guest goal pre-fills onboarding (`initialGoal`). Guest chat stays stateless. `guestState.test.mjs`.
  - **List durable + server-gated** (`51e2b73`): was localStorage-only with a client `premium` prop defaulting true (not durable, not monetizable, trivially bypassed). Generation moved to `server/lib/list.js` (authoritative) behind the new `shopping_lists` table + `/api/list` routes (`GET`/`POST`/`/rebuild`/`/swaps`). Inputs (goal/focuses/hard lines) + premium read from the **DB, never the request body** — free = basic goal template minus hard lines + a capability nudge; premium = focus-aware items + haul-swap integration, so a tampering client can't obtain them. Focuses now shape the list (were ignored); haul swaps queue via `/api/list/swaps` and merge into an already-saved list on next load. localStorage is now a cache; demo mode keeps a mirrored local generator. `list.livetest.js` (gating/focuses/tamper/dedup).
  - **Fail-open premium defaults fixed** (`8db8991`): Sidebar + List `?? true` and Sidebar's `premium = true` default rendered the premium variant during the load window — an entitlement leak. Now default to **NOT premium** until `getSubscription()` resolves. (Demo generator's `premium = true` is an intentional sandbox default, always passed explicitly in real paths.)
  - ℹ️ **Known-dead** (left as-is, flagged for future sessions): `client/src/lib/api.js` `sendWeightLog` + `POST /api/weight` have **no caller** — explicit weight logging is reachable only through chat NL (premium-gated).
  - ⚠️ **Live-smoke still pending** (no Supabase in this env): the new `shopping_lists` migration must be applied; onboarding→trial grant, guest-scan replay, and list persistence are verified by unit/logic tests + client build, not yet against a live DB.
- ✅ Block G — **trial decoupled from goal-set** (this commit), reversing the Block F "ANY goal-set routes through the trial-granting path" decision. Two ruled points:
  - **(b) List freemium — kept as built** (user-approved): free-known gets the basic goal-templated list; focus-awareness + haul-swap integration stay premium.
  - **(a) Goal-set no longer grants the trial — REVERSED.** The Block-F coupling silently killed the 3-free-notes taste mechanic (a trialing user is premium, so `free_notes_used` never increments) and spent a weekly-cadence 7-day trial on a casual goal-tap. Now: setting a goal (`/api/onboarding/coach`) — and completing the TDEE macro setup (`/api/onboarding/full`) — persist prefs but grant **nothing**. `ensureTrial` moved to a single explicit door, `POST /api/subscription/trial` (still idempotent: an existing trial/paid/expired row is returned untouched, so a consumed trial can't restart and a member's state is never disturbed). Client `startTrial()` + `handleStartTrial` (the one grant path, recomposes the blocked read in place, no taste spent). Offered from the **withheld-read slot** ("Start my free week" when `trialEligible` = never-had-a-row) and the **Upgrade screen** (primary CTA, paid demoted to "or subscribe now"). `trial.livetest.js` proves the counter decrements for a goal-set free user, the grant lifts the gate, idempotency, and the two-surface wiring; premium + 35 server tests + client build green. ⚠️ DB-persist path still not live-exercised (no Supabase in env).
- ✅ Block H — **CONSTRAINTS, the fourth preference dimension** (this commit): the shopper's real-life circumstances, orthogonal to goals/focuses and composing freely with them. Four dimensions now: Goals (shopping toward) · Focuses (watch) · Hard Lines (refuse) · **Constraints (what you're working with)**.
  - **Restructure**: two entries that were mis-filed as GOALS — `budget_clean` + `kids_snacks` — are circumstances, not directions, and are RETIRED as goals. `migratePreferences()` (server) / `resolveConstraints()` (client) resolve them at READ time → `goal=eating_cleaner` + the matching constraint (`budget` / `picky_kids`), so existing rows need no data backfill. "Feeding a family" stays a goal (whose cart); "picky kids" is a constraint on it.
  - **The 5 constraints** (`taxonomy.js` / `coachGoals.js`, kept in sync): `budget` · `short_on_time` · `picky_kids` · `no_kitchen` · `cooking_for_one`. Multi-select, optional, never pre-checked, no disclaimer (they're not health). Persisted to `user_goals.constraints` (new column); intake in coach onboarding (step 3) + the switcher's "What are you working with?" section + the free-text mapper (enum-guarded, invented constraints dropped).
  - **Where they apply**: LIST **heavily** (`CONSTRAINT_ITEMS` pull cheap/no-prep/kid-friendly/portioned staples, premium-gated like focuses; the intro names active constraints in her voice) and the NOTE **lightly** (emphasis + swap choice only). They are deliberately **never** passed to `evaluateIngredients` — a constraint can't move a tier or restore the seal. Premium capability (free lists ignore them).
  - **No price, ever**: budget = cost-conscious FOOD SELECTION, not a price lookup Kristy doesn't have. Enforced in the note prompt (`BUDGET IS FOOD SELECTION, NOT PRICE`, relative terms only) and structurally (no list item or intro carries a number/currency). Same discipline as the gluten-free advisory rule.
  - Verified: `constraints.livetest.js` (all 5 spec criteria: compose, migrate, budget-no-price, tier-safety, enum-only free-text) + `list.livetest` + `preferences.livetest` (incl. LIVE map of "cheap, no time, kids won't eat fish" → budget+short_on_time+picky_kids) + 37 server unit tests + client build. ⚠️ `constraints` column migration not yet live-exercised (no Supabase in env).
- ✅ Block I — **PERIMETER: a second knowledge base for the unlabeled store** (this commit). Kristy's sourced answers for the parts of the store with no barcode — the fish counter, the butcher, produce, dairy, the bulk bins, and what a label term actually means. The biggest gap in every scanner app; our own List is already full of perimeter items ("chicken or fish", "olive oil") she previously couldn't say a word about.
  - **Its own KB, never merged**: `server/kristy_perimeter_kb.json` (35 entries across seafood · beef · poultry_eggs · produce · dairy · bulk_pantry · label_terms), sharing the ingredient KB's four evidence tiers verbatim. Entries are TOPICS that answer a question, NOT flags — they're never fed to `evaluateIngredients`/the verdict engine, which is untouched.
  - **Claim-locked like the verdict note** (`server/lib/perimeter.js`): `sanitizeForModel` strips each retrieved entry to the SEVEN allowed fields (title/short_answer/detail/evidence_tier/buying_tips/labels_decoded/kristy_take) before the one Haiku call; `sources`, `question`, `aliases`, `id`, `category` never reach the model, so a planted fact can't be echoed. No-treatment rule absolute (mercury/raw-milk = information, never a directive); tiers honest (her standard ≠ consensus); no price; honest no-answer on a miss (never improvises).
  - **Raw milk** handled per spec: balanced, non-advocating — why people choose it (respected), the honest nutrition picture (difference smaller than claimed; no more-nutrient-dense/lactose/gut-colonization claims), the documented pathogen risk stated plainly with the vulnerable groups (established), the legal reality, and no directive either way.
  - **Served** via `POST /api/perimeter/ask` (authed; deterministic keyword/alias matcher → free entry content for everyone, PREMIUM personalized answer filtered through goal/focuses/hard-lines/constraints, with an optional list `refinement`) + public `GET /api/perimeter` + `GET /api/perimeter/:id` (free KB reads, SEO/acquisition, like ingredient pages).
  - **Chat × List loop**: every List item gets an "Ask" affordance → `PerimeterAsk` sheet → the personalized answer can REFINE the item in place ("Olive oil" → "Fresh, dark-bottle EVOO"), persisted. "Ask about the aisle" entry on the Scan surface for when there's nothing to scan. Free = the universal entry; personalized read + refinement = premium.
  - Verified: `perimeter.test.js` (10 unit: claim lock, matcher, honest no-answer, raw-milk balance, prompt verbatim) + `perimeter.livetest.js` LIVE (salmon → budget-personalized, no price, refinement "Frozen wild Alaskan salmon"; raw milk → names risk, no cure claim; planted fact never surfaces) + 47 server unit tests + client build.
- ✅ Block J — **launch pricing wired end to end** (this commit). Price *ids* are configuration, never hardcoded, and the client never sees them: server reads `STRIPE_PRICE_MONTHLY`/`STRIPE_PRICE_ANNUAL` from env (`priceIdForPlan` resolves a plan name → id); mobile reads RevenueCat entitlement/offering/package identifiers from `EXPO_PUBLIC_REVENUECAT_*` (`config.ts`), defaults matching a conventional dashboard setup.
  - **Single source for displayed prices**: new `client/src/lib/pricing.js` + `mobile/src/lib/pricing.ts` hold the $7.99/month · $59.99/year strings with the "About $5/month" framing on annual; no price literal exists anywhere else in either client (grep-verified). Numbers kept in sync across the two modules + the Stripe dashboard.
  - **Upgrade CTA logic**: trial primary ("Start my free week") when the user has never had a subscription row; "or subscribe now" secondary carrying both plan options; once the trial is consumed/active the plans become primary ("Start coaching — $59.99/year"). Prices in her voice, no fake urgency (factual "Save 37%" only).
  - **Checkout fails LOUD, never a dead button**: Stripe checkout logs exactly which env var is unset and returns a Kristy-voiced 503 (`billing.js` → `missingStripeConfig`); mobile logs a named error when a configured offering/package can't resolve and shows the same Kristy-voiced line instead of a silent broken CTA.
  - Verified: web `vite build` + mobile `tsc --noEmit` clean; 47 server tests; env-resolution smoke (unset → all three vars named + empty price id; set → resolves both plans). ⚠️ Live Stripe/RC dashboards not creatable from here — products to create are handed to the user (see below).
- ✅ Block K — **chat rewritten from calorie-tracker to GROCERY COACH** (this commit). `prompts.js` `CHAT_SYSTEM_PROMPT` was the pre-repositioning persona (macro accounting, the "optimization loop"); it's now a food/shopping coach whose first-class jobs are judging products, suggesting swaps, shaping the list, and answering no-barcode questions.
  - **Macros are OPT-IN, OFF by default** (the actual bug). New `user_goals.macro_tracking` (boolean, default false; defensive read tier in `getFullProfile` so a pre-migration DB keeps its coach prefs and just reads OFF). OFF: a reported meal ("I had chicken and rice") gets a coach's read — never a macro breakdown or a "logged it" card; `chatEngine` strips any macro the model slips in (structural, not just prompt). Explicit macro asks get a brief answer + a ONE-TIME "turn it on in Settings" pointer. ON: the full meal-logging + weight-optimization pipeline returns unchanged. The flag rides into the chat context so the model knows its mode.
  - **She speaks through the shopper's prefs**: `buildPreferencesBlock` renders goal/focuses/hard-lines/constraints (via the taxonomy labels, retired goals migrated) into every turn, framed as the shopper's own choices (no-treatment).
  - **Perimeter routing under the claim lock**: a no-barcode QUESTION ("is wild or farmed salmon better?") routes to the perimeter KB (`matchEntries` → free entry read, or premium `composeAnswer`) instead of the model's own knowledge — never hijacks a meal statement or a list command (`lib/chatRouting.js`, conservative). Guest chat is always coach mode (guests can't opt into macros).
  - **Hard rules restated** in both modes: claim lock, no-treatment (symmetric — no cause OR cure), tier-marked opinions, the fat philosophy (industrial vs whole), no price, no moralizing. Rate limiting / memory window / error handling untouched (route wrappers unchanged).
  - Verified: 7 new unit + 54 server tests green; `groceryChat.livetest.js` LIVE (12/12): OFF → coaching + no macros/log; ON (USDA stub) → logs with authoritative totals; salmon → tiered, budget-aware perimeter answer, no price/cure; "what should I buy this week" → moves toward building dinners. ⚠️ `macro_tracking` migration + the Settings toggle that flips it land with Block L (Settings restructure); DB-persist path not live-exercised (Supabase not writable here).
- ✅ Block L — **the fitness ghost removed from the default experience + color unified** (this commit).
  - **The macro-tracking master switch**: the client's `macroTracking` now reads the `macro_tracking` boolean (OFF by default) instead of "did they do the TDEE intake." `loadProfile` reads it defensively (falls back if unmigrated); `saveProfileFields` persists it (RLS, user's own row) — no new endpoint (server reads it via `getFullProfile` from Block K).
  - **The persistent nudge is gone**: the "Counting calories & macros too? Turn on Macro tracking" line was deleted from the sidebar chrome (rendered on every screen). It now lives ONLY inside Settings.
  - **Settings is a grocery user's screen**: Membership · **Shopping preferences** (goal/focuses/hard-lines/constraints summary + Edit → the goal switcher) · a single **Macro tracking (optional)** toggle · Account. Macro goal / Weight units / Training render ONLY when the toggle is on. Shopping preferences are the primary content, not the training split.
  - **No macro UI leaks when off**: Sidebar (macro section), TopBar (kcal pill), Settings (fitness sections), and the chat MacroCard are all gated on `macroTracking`; `MessageBubble` defaults it OFF so a stale/demo/guest message can't surface calories. Server already returns `macros:null` when off (Block K), so production is airtight.
  - **Color unified on the dark ramp**: web tokens + `mobile/src/theme/colors.ts` were already byte-identical (`#0B1F0F` ground · `#122718`/`#1A3320` cards) — confirmed, not one flat green (cards already lift on `surface`). Formalized the ramp: added `--bg-deep #040805` as the page void (matches the existing `theme-color`), keeping `.app` at the `#0B1F0F` ground. Tokenized every remaining hardcoded literal in `index.css` (error/danger/black/scrims → CSS vars mirroring `tokens.js`) and the one component literal (`#6B9E85` → `colors.trendMuted`); only black-alpha *shadows* remain as literals. `#040805` void + `#0B1F0F` ground + `surface`/`surface-2` cards = a real 3-step elevation.
  - Verified: `vite build` clean; token parity check (web ground/surface == mobile); no non-token color remains in `index.css` except effect shadows.
- ✅ Block M — **the camera viewfinder is text-free** (this commit): removed the Kristy-voice tagline ("Line up the barcode — I've got the rest") and its dead CSS from `CameraModal`. The live view is now only the feed + four gold corner brackets marking where to place the barcode/label; the sole control is the minimal top-right close (barcode capture is automatic). All inviting/instructional copy already lives on `ScanHome`, before the camera opens. `vite build` clean.
